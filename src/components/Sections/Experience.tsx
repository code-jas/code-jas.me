import React from 'react';

const Experience: React.FC = () => {
   return (
      <section
         id="technologies"
         className="bg-surface-2 flex items-center justify-center h-screen p-5 font-bold uppercase text-2xl"
      >
         Experience Content
      </section>
   );
};

export default Experience;
